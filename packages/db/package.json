{
  "name": "@mnemis/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate && bun run post-migrate",
    "push": "drizzle-kit push && bun run post-migrate",
    "post-migrate": "node --experimental-strip-types scripts/post-migrate.ts",
    "bootstrap": "node --experimental-strip-types scripts/bootstrap.ts",
    "studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.6.3"
  }
}
